<h1> Ruby Projects </h1>

<div id = "ruby_projects">
	<ul>
		<% @projects.each do |project| %>
			<li><%=link_to "#{project.name}", ruby_project_path(project.id), class: "single_project" %></li>
		<% end %>
	</ul>
</div>